import {
  capitalizeString
} from '../../../helpers/capitalize-string';
import { module, test } from 'qunit';

module('CapitalizeStringHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = capitalizeString(42);
  assert.ok(result);
});
