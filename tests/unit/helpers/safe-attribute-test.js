import {
  safeAttribute
} from '../../../helpers/safe-attribute';
import { module, test } from 'qunit';

module('SafeAttributeHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = safeAttribute(42);
  assert.ok(result);
});
